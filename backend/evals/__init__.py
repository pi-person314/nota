"""LLM command-interpretation eval suite.

Scores the real command orchestrator (`nota.orchestrator.loop.run_command`)
against a dataset of spoken-command transcripts with known expected
tool-call behavior. See `evals/README.md` for how to run it.
"""
